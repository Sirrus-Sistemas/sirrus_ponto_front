# Renomeie para `.env.local` (recomendado: já é ignorado pelo padrão *.local) ou `.env`
# e ajuste a URL do backend (sem barra no final). Backend padrão: PORT=3000, rotas /api/...
VITE_API_URL=http://localhost:3000
